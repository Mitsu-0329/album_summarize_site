<div class="row">
  <div class="col">
    <div class="form-body">
      <% if @post.errors.any? %>
        <ul class="alert alert-danger" role="alert">
          <h6 class="alert-heading">
            <%= @post.errors.count %>件のエラーが発生しました
          </h6>
          <% @post.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      <% end %>
      <%= form_with model: post do |f| %>
        <%= f.file_field :post_image, accept: "image/*", class: "w-100" %>
        <div class="input-group mb-3 mt-4">
          <div class="input-group-prepend">
            <span class="input-group-text" aria-hidden="true"><i class="fas fa-building"></i></span>
          </div>
          <%= f.text_field :title, class: "form-control", placeholder: "タイトル" %>
        </div>
        <%= f.text_area :body, class: "form-control mb-3", rows: "3", placeholder: "本文" %>
        <%= f.collection_select :genre_id, Genre.all, :id, :genre_name, { include_blank: "選択して下さい" }, { class: "form-control" } %>
        <hr>
        <%= f.submit '投稿', class: "btn btn-lg btn-block btn-info mt-4" %>
      <% end %>
    </div>
  </div>
</div>